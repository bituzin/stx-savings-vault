// frontend/src/index.js

import React rom 'react';
impotReacM rom 'react-dom/client';
import App fro'./p';
const root  ReactDOM.createRoo(documentgetElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
